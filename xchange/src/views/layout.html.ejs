 
<!DOCTYPE html>
<html>

  <head>
    <meta charset="utf-8">
    <title>X-Change</title>
    <% if (env !== 'development') { %>
      <link rel="stylesheet" href="<%= assetPath('app.css') %>">
    <% } %>
  </head>

  <body>
    <header>
        <img src="<%= assetPath('x-change 2.png') %>" alt="Logo" height="50" />
        <nav>
          <ul class="nav_links">
            <li><a href="PublicationsPath()">Publications</a></li>
            <li><a href="#">Negotiations</a></li>
            <li><a href="#">Profile</a></li>
          </ul>
        </nav>
    </header>
    <%- body %>
    <script src="<%= assetPath('app.js') %>"></script>
  </body>
</html>
